# devfootnote — 개발 메모와 근거 자료를 다시 찾는 개인 자료함

devfootnote는 개발 중 남긴 기술 메모와 저장한 근거 자료를 기억나는 단서로 다시 찾고, 당시 메모와 원문을 분리해 확인하며, 명시적으로 요청할 때만 저장 근거에 제한된 답변을 제공하는 개인용 웹 제품이다.

이 문서는 제품 선택의 배경과 활성 범위를 간결하게 설명한다. 규범적 요구사항과 수용 기준은 [`docs/spec.md`](docs/spec.md), 구현 순서와 gate는 [`docs/plan.md`](docs/plan.md), 기술 결정은 [`docs/adr/`](docs/adr/)가 소유한다.

## 1. 해결할 문제

개발 과정에서 참고한 문서의 일부, 장애 원인, 선택 근거와 개인 메모는 여러 위치에 흩어진다. 몇 주 뒤에는 정확한 제목이나 URL보다 “중복 확인만으로는 부족했고 DB 제약이 필요했다” 같은 한영 혼합 단서만 기억나는 경우가 많다. 일반 북마크는 이 단서로 근거 구간을 찾기 어렵고, 생성형 답변만 제공하면 저장한 사실과 모델의 추론을 구분하기 어렵다.

devfootnote의 핵심 가치는 세 가지다.

1. 본문만으로도 자료를 즉시 저장하고 현재 revision을 잃지 않는다.
2. AI 없이도 keyword 검색과 원문 읽기가 항상 가능한 기준선을 만든다.
3. 의미 검색과 답변을 사용할 때는 어떤 저장 근거를 썼는지 인용으로 확인하게 한다.

시장 독창성이나 범용 지식 관리 플랫폼을 목표로 하지 않는다. 작은 개인 corpus에서 검색 품질, 비동기 인덱싱의 정합성, 근거 답변의 실패 처리를 재현 가능하게 증명하는 데 집중한다.

## 2. 사용자와 대표 여정

production 사용자는 설정된 Google OIDC `(iss, sub)`와 정확히 일치하는 소유자 한 명이다. 공개 가입, 초대, 역할, 팀과 공유 권한은 없다.

대표 여정은 다음과 같다.

1. Google OIDC로 로그인한다.
2. 자료함에서 기술 메모, 근거 본문, 선택적 제목·출처 URL·맥락을 저장한다.
3. 기억나는 한국어·영어·식별자 단서로 keyword 검색한다.
4. 검색 결과에서 개인 메모와 저장 원문을 구분해 읽고 출처를 확인한다.
5. 동의와 예산이 허용된 경우 의미 검색을 사용하거나 근거 답변을 명시적으로 요청한다.
6. 자료를 수정하거나 삭제하고 최신 revision과 인덱싱 상태를 확인한다.

AI 설정, Redis 또는 worker가 없어도 2~4단계는 끝낼 수 있어야 한다.

## 3. 제품 표면

로그인 진입 외 제품 화면은 **정확히 세 개**다.

### 자료함 `/library`

- 자료 목록, keyword 단서, source와 최근 수정 상태를 보여 준다.
- 같은 화면의 form 또는 panel에서 자료를 등록·수정한다.
- 빈 상태, 저장 중, 저장 실패, revision 충돌과 임베딩 대기·실패·재시도 상태를 설명한다.
- 저장 실패나 충돌 시 작성 중인 입력을 보존한다.

### 검색·질문 `/search`

- keyword 검색을 기본으로 하고 준비된 자료에 한해 vector 또는 hybrid 검색을 구분해 제공한다.
- 필터, 검색 방식, 의미 인덱스 준비 범위와 fallback 이유를 노출한다.
- 답변은 사용자가 별도로 요청한 경우에만 만들며 각 주장에 자료·revision·근거 구간을 연결한다.
- 답변 대기·실패·만료·복구 상태를 제공하되 장기 채팅 목록은 만들지 않는다.

### 자료 읽기 `/documents/[materialId]`

- 개인 메모, 저장 원문, source URL, revision과 의미 인덱싱 상태를 구분해 보여 준다.
- 수정·삭제와 검색 결과의 근거 구간 이동을 지원한다.
- 삭제되었거나 권한이 없거나 revision이 바뀐 인용은 정상 자료처럼 표시하지 않는다.

모든 화면은 keyboard focus, 명시적 label과 error, screen-reader name, non-color status cue, reduced motion, 최소 44×44 touch target을 제공한다. tablet에서는 navigation을 collapse하고 mobile에서는 단일 열로 재배치한다. hover만으로 기능을 전달하지 않는다.

## 4. 저장·검색·답변 원칙

### 저장과 revision

저장 성공은 PostgreSQL transaction commit으로 정의한다. 현재 material revision, 원문, chunk, keyword index, idempotency 기록과 provider-agnostic immutable embedding intent를 하나의 원자적 저장 경계 안에서 준비한다. 같은 요청의 재전송은 중복 자료를 만들지 않고, 같은 idempotency key의 다른 입력과 stale expected revision은 충돌로 거부한다.

임베딩은 commit 이후 비동기로 실행한다. AI 설정·동의 전에 저장한 intent는 실행 상태 없이 남으며 저장·keyword 검색·읽기를 막지 않는다. 나중에 사용자가 기존 자료함 또는 검색 화면에서 의미 검색을 활성화하거나 현재 자료를 재시도하면 current revision·설정·동의 epoch·예산을 확인해 새 indexing work를 만든다. 기존 intent와 terminal work는 변경하지 않고 activation idempotency로 반복 요청의 중복 work를 막는다. 재동의도 새 epoch의 명시적 활성화로 처리하며 새 운영 화면이나 범용 재색인 시스템은 만들지 않는다.

Redis와 BullMQ는 전달 수단이며 job 완료를 publication 증거로 사용하지 않는다. 현재 owner·revision·삭제·설정·동의 epoch와 lease fence가 일치하는 전체 vector만 PostgreSQL에 게시한다. 늦은 worker 결과는 새 revision을 덮지 않는다.

### 검색

keyword 검색은 저장 직후의 현재 revision에서 사용할 수 있어야 한다. vector 검색은 해당 revision과 설정의 embedding이 모두 게시된 자료에만 사용한다. hybrid 결과는 owner/current/deleted predicate를 SQL 수준에서 적용하고 중복 근거를 안정적으로 합친다. 질의 임베딩 실패·미동의·예산 차단은 keyword fallback 사유로 명시하며 정상 hybrid 결과나 “근거 없음”으로 위장하지 않는다.

### 근거 답변

답변 생성은 명시적 요청, 외부 전송 동의, provider 정책과 예산 gate를 모두 통과해야 한다. 요청은 `REQUESTED → RESERVED → RUNNING → terminal`로 전이한다. 호출 전 durable request, evidence snapshot, consent epoch와 budget reservation을 기록하고, 별도 짧은 transaction이 `RUNNING`과 call-attempt 시작을 기록한 뒤 provider를 DB transaction 밖에서 호출한다. 호출 뒤에는 게시 fence와 비용 exposure를 독립적으로 판정한다. stale·삭제·동의 철회로 답변을 보여주지 않아도 이미 발생했거나 발생 가능성을 배제할 수 없는 비용은 한 번만 보수적으로 정산한다.

답변 저장 뒤 HTTP 응답이 유실되면 같은 owner·request ID·입력의 재요청은 revision·삭제·동의·만료를 다시 확인해 저장 결과를 반환하고 provider를 다시 호출하지 않는다. 같은 ID의 다른 입력은 충돌한다. Answer 본문과 citation의 짧은 보존 기간과 content-free 중복 호출 방지 receipt의 더 긴 보존 기간을 분리한다.

모델은 제공된 근거 밖의 사실을 확정하지 않는다. 인용은 요청 당시 evidence snapshot에 속한 표시 가능한 구간이어야 한다. 근거가 부족하거나 검증에 실패하면 답변을 게시하지 않고 제한을 설명한다.

## 5. 개인정보와 인증

- Google OIDC는 Authorization Code+PKCE, state, nonce, discovery/JWKS, signature와 issuer/audience/time을 서버에서 검증한다.
- callback과 configured `(iss, sub)` 검증 전에는 account 또는 refresh token row를 만들지 않는다. email, domain, 표시 이름이나 first-caller를 admission 기준으로 쓰지 않는다.
- browser에는 Google provider token을 저장하지 않는다. 성공한 callback은 pre-auth ID를 폐기하고 서비스용 access JWT와 refresh JWT를 발급한다. 둘 다 HTTPS same-origin의 HttpOnly cookie로만 전달하며 세부 cookie·CSRF 계약은 [ADR-0005](docs/adr/0005-google-oidc-single-owner-and-sessions.md)를 따른다.
- access JWT는 15분, refresh JWT는 로그인부터 7일의 고정 만료를 사용한다. PostgreSQL에는 owner당 현재 refresh의 SHA-256 해시와 만료만 최소 저장하고 재발급 시 원자적으로 교체한다. 회전으로 7일 기한을 연장하지 않으며 새 로그인은 기존 refresh를 교체한다.
- logout은 owner의 현재 refresh를 삭제하고 cookie를 지운다. 이미 발급한 access JWT는 만료까지 유효할 수 있다는 제한을 명시하며, 즉시 access 철회를 위한 denylist·Redis 인증·idle touch·장치 관리 기능은 만들지 않는다.
- owner predicate는 material, revision, chunk, search, job, answer, citation과 usage query에 적용한다. client가 보낸 owner ID나 검색 후 application filter만 신뢰하지 않는다.
- secret, cookie/token, 사용자 원문, 질문, prompt, answer와 raw provider response를 log, trace 또는 metric label에 기록하지 않는다.
- 외부 AI 전송은 기본 off다. 동의 철회, 삭제와 보존 만료는 새 호출과 게시를 차단하고 정해진 수명주기로 데이터를 제거한다.

## 6. 제품 및 기술 범위

고정 기술 스택은 다음과 같다.

- **Workspace/runtime:** pnpm, Turborepo(Phase 0부터 최소 task 구성), Node.js LTS, strict TypeScript
- **로컬 개발·이미지 검증:** 루트 `docker-compose.yml`로 PostgreSQL+pgvector 실행, Phase 2부터 Redis 추가. 평소 web/API/worker는 호스트에서 pnpm·Turbo로 실행한다. `app` profile에서는 앱별 Dockerfile로 빌드한 runtime image로 전체 실행을 확인하고, 자동화된 DB·큐 통합 테스트는 개발 데이터와 분리된 Testcontainers를 사용한다.
- **Frontend:** Next.js App Router, React, Tailwind CSS, shadcn/ui와 Radix, TanStack Query, React Hook Form과 Zod, `react-markdown`과 `rehype-sanitize`, Lucide
- **Backend:** NestJS modular monolith, PostgreSQL과 pgvector, `pg`+`drizzle-orm/node-postgres`, `drizzle-kit` SQL migrations, Redis와 BullMQ
- **Contract:** Swagger, 공통 성공 `success/data`·오류 `success/error/message` JSON, checked-in OpenAPI, `@hey-api/openapi-ts`; AlmondYoung의 작은 interceptor/filter 책임 분리를 참고하며 상세 계약은 ADR-0007
- **Auth:** 유지보수되는 표준 OIDC client, `@nestjs/passport`·`passport`·`passport-jwt`·`@nestjs/jwt`, `JwtStrategy`·`JwtAuthGuard`·`RefreshJwtGuard`, 서비스용 access/refresh JWT
- **Verification:** Vitest, React Testing Library, MSW, Supertest, Testcontainers, Playwright
- **Operations:** Nest 기본 기동 로그, 공유 Pino logger, 단순 liveness 응답과 Terminus 기본 API의 DB readiness. 라이브러리를 제거하기보다 직접 만든 주변 계층을 줄이고, OpenTelemetry·요청 추적은 Phase 2 이후 실제 관측 필요에 맞춰 도입한다.

실행 앱은 `apps/web`, `apps/api`, `apps/worker`로 나누고, API와 worker가 사용하는 업무 규칙은 `packages/backend`의 `identity`, `materials`, `indexing`, `search`, `answers`, `usage-retention`에 둔다. 웹은 `packages/api-client`의 생성 HTTP client만 사용한다. 모노레포 자체가 업무 코드를 반드시 packages에 두도록 요구하는 것은 아니며, 이 프로젝트는 API·worker의 규칙 중복을 막고 실행 수명주기를 분리하기 위해 이 경계를 선택한다.

백엔드는 **기능별 모듈러 모놀리스 + 간단한 계층 + 필요한 곳만 port**를 사용한다. API의 `src/auth`, `src/materials`, `src/search`, `src/answers`에는 Controller·DTO 등 transport를 두고 `src/common`에는 작은 interceptor·filter·decorator를 둔다. 업무 feature 내부는 Nest Module·Service·Repository를 기본으로 하며 public port는 명명된 Service 메서드와 명시적인 Module export로 구현할 수 있다. 모든 기능에 interface·UseCase·mapper를 만들지 않으며 다른 feature의 private repository·schema 접근은 계속 금지한다. 앱은 composition·transport·lifecycle을, backend feature는 업무 규칙·transaction을 소유한다. worker의 processor·scheduler는 worker에서만 등록한다. 구체적인 목표 폴더 구조와 허용 의존성은 [ADR-0001](docs/adr/0001-monorepo-and-package-boundaries.md), 소유권과 계층 기준은 [ADR-0002](docs/adr/0002-modular-monolith-domain-ownership.md)를 따른다.

Docker는 개발 의존성 실행뿐 아니라 앱 이미지 빌드, CI의 최소 실행 검증과 중지·재시작 복구 확인에도 사용한다. web/API 이미지는 Phase 1, worker 이미지는 Phase 2에 도입한다. 모노레포 루트를 build context로 사용하고 multi-stage·non-root·secret 제외를 적용한다. 개발 데이터는 named volume으로 보존하며 migration은 앱 시작과 분리해 명시적으로 적용한다. 호스트 개발과 앱 컨테이너 검증 모두 same-origin `/api/v1`·HTTPS cookie 인증을 유지하고 Redis 장애가 API 기동·저장·keyword·읽기를 막지 않아야 한다. 초기에는 원격 Turbo cache, 별도 UI·Tailwind·config package 또는 개발용 오케스트레이터를 추가하지 않는다. 이 로컬 검증 계획은 registry push나 AWS 배포 승인이 아니다.

Drizzle은 학습 경험이나 유행이 아니라 immutable revision/chunk 저장, 조건부 상태 갱신과 keyword/vector 순위 조합을 같은 SQL 중심 방식으로 다루기 위해 선택한다. 일반 조회·저장은 typed query로 구현하고 검색·잠금 등 필요한 구간만 parameterized `sql` 템플릿으로 보완한다. 프론트에는 DB schema가 아닌 OpenAPI 기반 client를 공유한다. 프로세스별 단일 연결 풀·종료 처리, 같은 `tx` 전달, 생성 SQL/metadata 검토·단일 실행 주체의 명시적 migration 및 DB 오류 정제 기준은 [ADR-0003](docs/adr/0003-postgresql-pgvector-drizzle-revisions-and-search.md)을 따른다. ORM 선택과 별개로 원자 저장·멱등성·revision fence를 실제 PostgreSQL에서 검증하는 것이 핵심이다.

Exact package version은 별도로 승인된 scaffold 시점에 호환 가능한 stable 버전으로 고정한다. AI provider, model, version과 embedding dimension은 문서 단계에서는 정하지 않되 각 purpose의 첫 실제 호출 전에 품질·privacy·비용 검토와 별도 승인을 거쳐 고정한다. Phase 6은 AI 구성을 다시 선택하는 단계가 아니라 AWS topology와 운영 비용을 결정하는 단계다.

## 7. 디자인 방향

루트 [`DESIGN.md`](DESIGN.md)의 `Notion Analysis`를 repository-owned 디자인 참고 계약으로 사용한다. 따뜻한 `#f6f5f4` canvas, white surface, near-black/warm-gray text, `#e6e6e6` hairline, 절제된 radius와 elevation, 8px 기반 spacing, `#0075de` primary와 `#005bab` pressed 관계를 devfootnote의 navigation, card, input, modal, empty state와 toast에 적용한다.

글꼴은 Notion 전용 자산이 아닌 **Inter**를 사용한다. Notion의 이름, logo, product copy, 이미지, sticker, 고유 asset을 복사하거나 제품을 사칭하지 않는다. 상태는 의미 token과 text/icon/ARIA를 함께 사용하고 색만으로 전달하지 않는다. 일반 text 4.5:1, large text·UI·focus 3:1의 WCAG AA 기준을 지킨다.

## 8. 검증할 가치

첫 번째 완료 단위는 로그인한 소유자가 자료를 저장하고 Redis나 외부 AI 없이 keyword로 찾아 원문을 읽는 end-to-end 여정이다. 그 위에 비동기 embedding publication과 장애 복구를 더하고, 마지막으로 동의·예산·근거 fence를 갖춘 답변을 추가한다.

검색 품질은 versioned corpus와 고정 질문으로 keyword 기준선부터 측정한다. 개발용 20개 사례로 실패 유형을 찾고, 최종 평가는 동결한 40개 사례(근거 있음 32개, 근거 없음 8개)로 수행한다. Hit@1/5, multi-evidence recall, grounded/valid/false-answer ratio, latency와 실제 비용을 함께 기록한다. 성능 실험은 1천/1만 chunk를 분리해 측정하고 합성 자료로 의미 품질을 주장하지 않는다.

핵심 실패 시나리오는 중복 저장, 동시 수정, Redis 중단, worker crash와 late result, 삭제·revision 경쟁, provider timeout/429·잘못된 인용, 동의 철회, 예산 소진, JWT 만료·용도 혼용·refresh 재사용/동시 회전·logout, CSRF와 다른 owner 접근이다. access 만료 뒤 유효한 refresh로 복구할 수 있는지도 검증한다. 저장·검색과 refresh 교체의 데이터 계약은 실제 PostgreSQL로, queue 계약은 실제 Redis와 BullMQ로 증명한다.

## 9. 제외 범위

웹 crawler, browser extension, PDF·image OCR, YouTube 전사, 전체 GitHub code 분석, 장기 chat, 결제·구독, 추천 feed, 별도 mobile app, 팀·공유 권한, Agent·MCP, 지식 graph, 처음부터의 reranker·HNSW·고급 chunking·범용 workflow engine은 만들지 않는다.

문서 수정·리뷰 승인만으로 제품 code·manifest·migration·test나 DB·인프라를 변경·실행하지 않는다. 진행 중인 scaffold/구현의 승인 범위를 유지하며 Git 작업은 별도 명시적 승인 없이 하지 않는다. Terraform·AWS resource·deployment는 Phase 6의 별도 승인 전까지 제외한다.

## 10. 비용과 배포 gate

배포 topology는 아직 선택하지 않는다. Phase 6에서 다음 AWS 후보를 같은 workload로 비교한다.

1. 단일 저비용 Graviton EC2에 app/worker/PostgreSQL/Redis를 함께 두는 후보
2. EC2 app/worker/Redis와 managed PostgreSQL을 분리하는 후보
3. Fargate, managed PostgreSQL과 managed cache를 사용하는 후보

각 후보는 compute, database, cache, storage/IO, backup, public IPv4, registry, logs, DNS, load balancer, NAT와 transfer를 포함하고 공식 가격 URL·조회 시각·region·수량×단가를 기록한다. 무료 tier나 credit은 지속 비용에서 차감하지 않는다. 지정된 보수적 환율, 세금과 20% 여유분을 적용한 `finalTotalKRW`가 **30,000원을 초과하면 BLOCK**한다. 30,000원 이하는 비용 조건만 통과한 것이며, peak memory 여유와 restore rehearsal 및 별도 배포 승인이 추가로 필요하다.

Phase 6의 별도 승인 전에는 region, SKU, topology를 승인안으로 확정하지 않고 Terraform 파일, cloud resource 또는 deployment를 만들지 않는다. 비용 gate 통과도 자동 배포 승인이 아니다.

## 11. 현재 상태와 다음 승인 경계

현재 저장소는 Phase 0 scaffold 구현 중이며 제품 완성이나 테스트 통과를 주장하지 않는다. 기능 단위의 짧은 계획 → 구현 → 실제 검증 순서와 각 phase의 gate는 [구현 계획](docs/plan.md#61-기능-단위-작업-절차)을 따른다. 명령은 실제 manifest·설정과 검사 결과로 확인하며 문서 수정·리뷰 승인은 진행 중인 제품 코드·DB·Git·인프라 또는 배포 변경 승인이 아니다.
